# Migration

This is the initial `plane-workflow` release. There is no prior Plane-skill configuration to migrate.

Create an optional `plane-workflow.config.yaml` when profile defaults need to be customized; see [configuration](plane-workflow/configuration.md).
